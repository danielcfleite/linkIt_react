import styled from "styled-components";

export const RecentContactsContainer = styled.div`
  display: flex;
  flex-wrap: nowrap;
  width: 100%;
  gap: 1.5rem;
  overflow-x: scroll;
  overflow-y: hidden;
  padding: 20px 0;
`;
